export function FooterBar() {
  return (
    <div className="footer-bar">
      <div className="muted">Decisions are saved in this browser and matched back to mentees by email on every upload.</div>
    </div>
  );
}
